// Placeholder — Agent Task 3 will implement room loading
export function loadRoom(scene, roomId) {
  // TODO: Fetch room JSON, parse tilemap, create collision layer,
  //       spawn player at entry, spawn guards/lasers from object layer
}
