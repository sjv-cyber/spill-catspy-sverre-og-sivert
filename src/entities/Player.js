// Placeholder — Agent Task 1 will implement Player with dual-form physics
export class Player {
  constructor(scene, x, y) {
    this.scene = scene
    this.isHuman = true
    // TODO: Create sprite, physics body, state machine, input bindings
  }

  update(dt) {
    // TODO: Movement, jumping, transformation, wall climbing (cat)
  }

  transform() {
    // TODO: Toggle form, resize hitbox, apply physics changes
  }
}
