// Placeholder — Agent Task 2 will implement Guard AI
export class Guard {
  constructor(scene, x, y, config) {
    this.scene = scene
    // TODO: Create sprite, patrol waypoints, vision cone
  }

  update(dt) {
    // TODO: Patrol movement, vision cone check, LOS raycast
  }
}
