// Placeholder — Agent Task 2 will implement Laser hazards
export class Laser {
  constructor(scene, x1, y1, x2, y2, config) {
    this.scene = scene
    // TODO: Create line graphic, timer toggle, collision
  }

  update(dt) {
    // TODO: Toggle on/off per timer, check player overlap
  }
}
